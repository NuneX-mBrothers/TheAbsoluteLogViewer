# The Absolute LogViewer — Distribution Repository

This is the **public distribution repository** for *The Absolute LogViewer*, a modern WPF/.NET 10 log file viewer for Windows.

> **Note:** the source code is **not** in this repository. Only build artifacts and distribution scripts are public. This repo serves as the host for GitHub Pages (landing page) and ClickOnce manifests.

🔗 **Landing page:** https://nunex-mbrothers.github.io/TheAbsoluteLogViewer/
🔗 **Releases:** https://github.com/NuneX-mBrothers/TheAbsoluteLogViewer/releases

---

## Repository structure

```
.
├── index.html                  Landing page (GitHub Pages root)
├── LogViewer.application       ClickOnce deployment manifest
├── Application Files/          ClickOnce versioned manifests
├── Publish.html                Auto-generated by VS Publish (ClickOnce fallback page)
├── setup.exe                   ClickOnce bootstrapper (installs .NET if missing)
├── StandAlone/
│   └── version.json            Update metadata read by UpdateService
├── 1-prepare.cmd               Bumps version + regenerates version.json + copies index.html
├── 2-publish.cmd               Zips binaries + creates GitHub Release + git push
├── .gitignore                  Excludes large binaries from the repo
├── .gitattributes              Forces binary mode for ClickOnce files (prevents CRLF corruption)
├── .nojekyll                   Disables Jekyll on GitHub Pages
├── _headers                    Netlify MIME type rules (for ClickOnce content types)
├── netlify.toml                Netlify deployment configuration
└── social-preview.jpg          Open Graph preview image
```

---

## Distribution channels

The application is delivered in **three editions**:

| Edition | Where it lives | Size | Requirements |
|---|---|---|---|
| **ClickOnce** | This repo (`Application Files/` + `LogViewer.application`) | ~5 MB | Microsoft Edge |
| **Standalone** | GitHub Releases | ~1.3 MB | .NET 10 Desktop Runtime installed |
| **Portable** | GitHub Releases | ~158 MB | None — self-contained |

ClickOnce uses Microsoft's native auto-update mechanism via the `.application` manifest. Standalone and Portable use the custom `UpdateService` which reads `StandAlone/version.json` and downloads new binaries from GitHub Releases.

---

## Release workflow

```
┌──────────────────────────────────┐
│  Edit code in Visual Studio       │
│  (separate private repository)    │
└──────────────┬───────────────────┘
               ▼
┌──────────────────────────────────┐
│  Run 1-prepare.cmd                │
│  - Bumps version (csproj, pubxml) │
│  - Regenerates version.json       │
│  - Injects version into index.html│
│  - Cleans bin/obj                 │
└──────────────┬───────────────────┘
               ▼
┌──────────────────────────────────┐
│  Visual Studio: Publish 3 times   │
│  - ClickOnce profile              │
│  - Standalone profile             │
│  - Portable profile               │
└──────────────┬───────────────────┘
               ▼
┌──────────────────────────────────┐
│  Run 2-publish.cmd                │
│  - Creates zips                   │
│  - Renames Portable executable    │
│  - gh release create v{version}   │
│  - git commit + push              │
└──────────────────────────────────┘
```

---

## License

**Freeware.** See the [landing page](https://nunex-mbrothers.github.io/TheAbsoluteLogViewer/) for usage terms. Free for personal and commercial use.

Source code is **not open**. This repository accepts **bug reports** and **feature requests** via Issues, but pull requests are not accepted as the source code is not public.

---

© 2026 NuneX-mBrothers
